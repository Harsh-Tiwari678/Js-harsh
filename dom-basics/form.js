/*let nm = document.querySelector("#nm");
let form = document.querySelector("form");
form.addEventListener("submit",function(dets){
    dets.preventDefault(); // stopping from submitting it 
    if(nm.value.length<=2){
     document.querySelector("#dis").style.display = "initial";
    }
    else{
        document.querySelector("#dis").style.display = "none"
    }
}) // reading input 

 let nm = document.querySelector("#nm");
let form = document.querySelector("form");
form.addEventListener("submit",function(dets){
    dets.preventDefault();
    const regex =/^[a-zA-Z][a-zA-Z0-9_]{2,15}$/;  /// slashes at the front and the bottom are imortant 
     let ans =  regex.test("harhs_");
     console.log(ans);
}); // code for checking with regex ... 
*/

